#include <iostream>
#include "game.h"

using namespace std;

static int runApp(Application* app){
	Window window(*app, 800, 600, "Test iquna");
	RenderDevice device(window);
	Sampler samper(device, RenderDevice::FILTER_LINEAR_MIPMAP_LINEAR);

}
